/**
 * Product copy that appears in more than one place.
 *
 * The previous tagline — "run infinitely in the cloud. Powered by AI SDK,
 * Gateway, Sandbox, and Workflow SDK" — survived the rebrand in four files and
 * described a product this no longer is: there is no cloud, no Gateway, and no
 * hosted sandbox. Keeping it in one place means the next change lands once.
 *
 * It is also the first thing a stranger reads, so it names what they get, not
 * what it is built from. The parts list (Claude Code, Docker, Postgres) is
 * still true and still on the page — further down, where someone who cares
 * about it will look.
 */

/** One line, used in the hero, the OG image, and page metadata. */
export const PRODUCT_TAGLINE =
  "Describe what you want built, and an AI writes the code for you — on your own computer, where you can try it before you keep it.";

/** Slightly longer, for pages that have room to explain. */
export const PRODUCT_DESCRIPTION =
  "Paco turns a description of what you want into working code. It runs on your own computer, inside a sealed workspace that cannot reach the rest of your files, so you can preview the result, read exactly what changed, and save it to GitHub only when you are happy with it.";
